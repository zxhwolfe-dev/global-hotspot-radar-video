# Codex 复核后的增量审查与接续

代码基线：视频 `e1c0f75`；网站 `058f984e3e1b6171e38bc71321a244a7e7e84f2f`。提交期间视频分支新增 `ffc8d3d710ac6ca17d91dfe7f9c832aabb2b5460`，只添加本地复核记录；本轮以ffc8d3d为父提交接入，完整保留它，不强推、不合并main、不部署、不重做旧片。

## 先核对已完成与未完成

已读取 `LOCAL_VALIDATION_20260918.md`：本地报告记载全仓/安装路径143项通过、中文09-12及09-15完整副本终检通过。英文09-12是尚无素材的脚手架，73页字幕通过不代表英文MP4验收；报告明确英文完整成片未验证。网站058f984e只补旧夹具fact_basis，43项通过；保留修正，不撤回。

9个前端刷新失败也有了更明确的解释：本地记录称worktree缺node_modules/jsdom，属于环境性启动失败；主工作区8/9通过，另一项关联未推送的a2e09484布局改动。不能称为9个线上产品缺陷，也不能用环境失败当回归通过。本轮没有重新执行这些本地检查。

## [P1][字幕] 英文词边界被抹掉——已修

问题位置：`scripts/ghr_renderer/subtitle_validation.py` 的compact/expected_pages/check_srt。删除全部空白并直接拼接字幕行，使 `now here` 与 `nowhere`、`not able` 与 `notable` 被当成相同文本。

修改：新增caption_key，保留非汉字字母/数字间的词边界；行/页用边界拼接后比较。汉字正常换行兼容，大小写、小数点、百分号不丢失；compact只用于明确保护词定位。反例实测：原文 `It is now here.`、SRT `It is nowhere.`，旧版无错，新版报正文不一致。

风险：旧稿中拆开的英文词/数字可能被新检查识别，需定位cue，不得删除词间空格来求通过。它面向中英系列，不是所有语种分词器；标点旁空白仍按排版处理，不证明完整语义或烧录像素。

两个既有测试夹具同步修正而非删测：重叠测试从AB分页改成A B分页；V1三行测试从拆开的2026改成三个完整英文词。原时序、V1行数断言保留；另增测试确认AB/2026拆成新词边界应报文本错误，V1仍无两行硬限制。

## [P1][音频复用] 章节和原声内容不在完整校验范围——已修

问题位置：`scripts/ghr_renderer/audio_contracts.py / audio_fingerprint`。原指纹缺story_id，但build_audio按它安排章节音，复用timeline也携带它供转场使用。原声仅以绝对路径/大小/mtime识别，同大小保留mtime的替换漏检，内容不变但touch/复制却误失效。

修改：主音轨指纹算法升为 `global-hotspot-master-audio-v3`，加入含封面的timeline_story_ids，默认值与渲染器一致。原声使用大小+流式SHA-256；每次指纹计算每个唯一片源只读一次，不做跨调用mtime缓存。读前后比对元数据，发现普通并发修改就拒绝；路径仍限定在项目内。提供者tts_fingerprint保持v2、音色与导演说明不变。

反例实测：章节变更，旧版不失效/新版失效；同大小同mtime换内容，旧版不失效/新版失效；只touch不改内容，旧版失效/新版不失效；提供者TTS指纹前后相同。

风险与迁移：旧v2主音轨哈希首次再用 `--reuse-audio` 会被拒绝，这是预期安全失效，不损坏已有MP4。若提供者mp3、line_timings、tts_cache_meta齐全且TTS指纹匹配，可在副本用 `--reuse-tts` 重建混音/时间线，再复用新主音轨；必须确认未调用TTS提供者。缓存缺失时只报告，不自动付费调用，不手工改meta哈希，不静默回退v2，不批量重渲染旧片。

每次完整读取唯一片源有I/O代价，不宣称全面提速。只改主音轨指纹，渲染器大于24MiB的画面缓存签名仍用旧策略。哈希不证明音频内容正确、授权有效或整个项目已可迁移；读取后再改文件仍需生产端单写者约束。

## 实际测试范围

```
python -m pytest -q tests/test_episode_preflight.py tests/test_episode_validation.py tests/test_review_followup_contracts.py
127 passed
```

95项既有相关测试+32项新增测试，直接导入实际模块；含现有合成H.264/AAC媒体集成。新测试在原模块暴露反例，修后通过。`python -S scripts/preflight_episode.py --help` 与编译检查通过。没有调用真实TTS、联网或OCR。

原模块与既有测试从GitHub读取/上轮副本还原并核对Git blob SHA；未克隆完整仓库。本轮没有重跑35项研究测试、13项完整渲染器基础测试或历史中英成片。127不是全仓总数，不替代本地143项，也不证明真实听感或传播效果。

## 已读到、但本轮未修的更大缺口

1. **复用音轨后字幕样式可能仍旧。** build_audio写ASS，reuse_audio复用音轨/timeline/SRT，render_video烧录现有ASS。字体、字号、边距、保护词变化未在复用路径重建字幕资产。应抽出“已验证cue+当前排版→SRT/ASS”的确定性步骤，build/reuse共用；不要把字体塞进音频指纹逼迫重混音。测试应断言改字号时音轨哈希不变、ASS变化，缺ASS可重建，保护词重新折行，失败不覆盖正式文件。
2. **preview/candidate可能改写final旁挂文件。** main入口先build_audio，后者写final音轨/SRT/ASS/timeline；随后只有MP4按档位写preview。先用副本哈希复现“旧正式MP4+新旁挂文件”，再把一次build派生产物隔离暂存，成功验收后明确提交整组产物；不要只原子替换MP4，也不要自动清理旧正式版。
3. **网站坏缓存恢复不一致。** 已读058f984e：前两次缓存读取有schema失败保护，获取跨进程lease后的再次读取缺该保护；持续坏缓存可能在最后读取抛错而非重新生成。统一有效缓存读取，补坏缓存/其他进程刚写入有效缓存的测试，避免无条件删缓存误伤新值。另有单题锁先登记再抢全局容量、容量失败在清理finally外，以及同题等待者占用容量的问题；宜单题in-progress快速返回后再抢模型容量，保留跨进程lease。这里只确认代码路径风险，未实测线上重复计费，本轮未改网站。

## 给本地Codex：不要重做已经完成的排查

- 拉取原视频评审分支最新头，先读本文件。保留058f984e、ffc8d3d、已同步依赖及本地其他改动。独立worktree复核本次增量，先跑127项范围，再跑完整tests，报告实际数量。
- 在已有中英项目副本检查新词边界与v3迁移；不补造研究记录、不改历史审核、不自动重做旧片。缺英文完整成片就继续记未验，不用脚手架73页替代。
- 通过后增量同步至已确认的 `/home/zxhwolfe/.codex/skills/global-hotspot-radar-video/`，记录SHA与哈希；无需再寻找安装路径，不整目录覆盖、不删除缓存、不合并main、不部署。
- 之后优先修“复用时刷新字幕资产”和“preview不污染final”，各先加真实文件/哈希反例再提交。网站缓存与容量单独小提交。前端先确保worktree可加载jsdom，再以一致依赖分别跑main/PR；本地a2e09484由其所属工作流处理，不擅自覆盖。
- 功能提升应转向按需主动研究，而非继续堆检查表：在同一时间窗比较现有雷达与主动发现，保存池外发现、原文新增信息、候选取舍、查询成本和最终选题。服务化必须实际接入检索/原文读取与预算控制，不能把检索词生成当已搜索。本轮未实现该服务，不凑固定题数、不承诺爆款。

返回新提交/测试、缓存迁移、安装哈希及新修问题；已验与未验分开。研究记录、技术PASS、听感、美感和真实数据效果不能互相代替。
