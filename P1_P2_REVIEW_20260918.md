# ceacc08 / f0f53879 接续复核、P1/P2 修复与本地交接

视频基线：`ceacc08fb8c3bc0a848a1e968ce1f8a905f6ac36`，网站基线：`f0f538793ee230376b36b42bea5f031ac235eaca`。新工作使用独立分支和叠加PR，base是视频原PR #1分支；不修改原分支、main、部署或历史项目。此前主PR与main的冲突不通过强推解决。

## 1. 核对 Codex 提交：代码做了什么，测试实际证明什么

本次新消息列出了4个新增修复SHA，而不是5个；此前`058f984e`测试夹具修正也已复核并保留。没有猜测不存在的第五个提交。

| 提交 | 复核结论 |
|---|---|
| c407dfe | reuse_audio确实按当前字号/边距/保护词重写SRT/ASS。但build_audio仍保留重复代码，没有按提交说明与之共用helper；测试mock了render_video，没有验证真实烧录与封装。 |
| 8e0f630 | build_audio非release确实写preview，不改final旁挂；但reuse_audio从final选音轨后只写preview字幕，render_video却按返回目录找preview音轨，可能缺失或误用残留文件。原新增测试只覆盖mock渲染的reuse路径。 |
| ceacc08 | preview中手工准备的v3组能被reuse接受；测试没有执行实际reuse-tts重建和mux。release的两个候选目录都还是final，因此仅有preview产物的新项目不能按三档流程复用到release。 |
| f0f53879 | 网站新增helper保护了锁内和lease后的坏缓存读取，容量申请确实移到lease之后，嵌套finally释放。两个新增测试分别使用实际SQLite+fake模型，以及mock租约+容量计数器；符合两项测试目的，不证明全部线程/跨进程并发。网站45+143为本地报告，本轮未重新运行。 |
| 058f984e（先前修复） | 旧rich fixture补fact_basis与降级规则一致，保留；本轮未再次改网站。 |

原视频178项测试在本环境也能通过：缺少外部Akai provider，只为基线导入提供了会在配置或合成时抛错的占位模块，未执行模型。这再次说明“已有测试全绿”不能替代真实链路覆盖。修改版不再需要该占位模块。

## 2. [P1][整组音频交接] 真实失败反例与修复

基线实际调用render_video和FFmpeg（没有mock视频函数）：仅final组存在时，preview封装报找不到`preview/master_narration_with_tail.wav`。随后只保留preview组，release reuse_audio报缺文件。反例仅用合成色底与440Hz音调，不是新闻或真实TTS。

修改文件：`scripts/ghr_renderer/audio_artifacts.py`（新增）、`scripts/render_episode.py`。

- 每档优先当前目录，再检查另一个目录；必须整组指纹、卡片ID、连续时间线、有限时长与音轨长度检查通过才能选中。坏/过期的完整preview组不会遮住当前匹配的final组；两个都不匹配则报告原因，不自动合成。
- SRT/ASS是可再生资产，不再作为选择核心音轨的前置文件。timeline、WAV、sync report从同一有效组取得；跨目录时先把三者完整复制到临时目录，再逐文件替换，report最后写入。渲染器的字幕与音轨实际读取同一输出目录。
- build_audio和reuse_audio现在确实调用同一个字幕生成helper，报告中的subtitle_sidecar和master_audio指向实际使用的目录。
- 新建/暂存音频失败时保留`.audio-build-incomplete`标记。下次不把半完成组当有效缓存；可以使用另一个匹配组，或显式从保留TTS重建。绝不手改旧指纹、自动付费重试。

风险与边界：增加一次跨档位音轨复制和验证读取；不是全操作提速。三文件替换有失败标记，但不是整个release目录断电原子提交。release仍可能在最终视频成功前更新旁挂/覆盖同名视频；本轮未把全套发布事务列为已完成，已有作品修订继续使用副本与版本化文件。preview/candidate的final只读已在真实封装中检查；外部程序不遵守单写者约束时仍可能改文件。

## 3. [P2][离线依赖] 复用不应需要线上配音模块和密钥

问题：顶层import强依赖Akai的TTS provider，甚至`--help`也不能单独运行；build_audio在判断reuse_tts前就加载provider配置与.env。

修改：provider类型仅供静态类型检查；`_load_tts_provider`仅在新合成分支调用。缓存复用和`--reuse-tts`都不读.env、不导入provider、不触发网络；保留TTS v2/master v3指纹，不改变音色、指令或合成参数。新合成仍要求真实host provider与凭据，缺失时明确报错，不偷偷降级到别的声音。

build_audio的字幕输出去重也属于P2单一职责修正，避免以后只改复用路径或只改新合成路径。

风险：依然需要FFmpeg、Pillow、numpy、OpenCV、python-dotenv等现有渲染依赖；不是完全无依赖。离线分支报告声音身份来自匹配的缓存合同/manifest，不表示本次执行了提供者。新合成供应商真实连接本轮未验证。

## 4. [P1][单写者] 同一项目不能并行覆盖temp/audio

新增`ghr_renderer/episode_lock.py`，CLI在素材校验、配音、渲染和manifest更新期间持有同一项目锁。preview/candidate/release同目录第二次进入立即失败，不删除在用临时目录，不消耗TTS；不同项目互不阻塞。

锁文件`.ghr-render.lock`保持存在，不能用删锁文件的办法抢锁。POSIX/WSL使用flock，Windows分支使用标准库字节锁；本轮实测的是Linux/POSIX，不声称Windows分支已实测。直接调用底层Python helper的宿主仍须提供同等单写者约束。本锁不是分布式锁；强杀父进程后若遗留FFmpeg子进程，恢复前仍须核查在途进程，不能仅凭锁空闲自动重做。

## 5. 实际验证

```text
原178项（原文件+拒绝调用的外部provider导入占位）：178 passed
修改版全部tests（不需要provider占位）：199 passed
新增 test_offline_artifact_roundtrip.py：21项，全部通过
```

原7个测试文件未修改，逐个核对Git blob SHA；全部生产依赖从GitHub读取或已验证工件还原并核对。不是完整git clone；未把网站大仓下载到本地。Python3.13/Linux；测试进程限制到2个CPU避免共享环境线程过量，此限制未写入生产代码。jieba不在本环境，使用渲染器已有fallback分词路径；不替代用户WSL中的真实中文排版验收。

新增测试覆盖：final→preview和candidate真实渲染、preview→release真实渲染/封面、暂存的错误频率音轨不能混入（解码AAC频谱核对440Hz）、过期缓存回退、缺SRT恢复、时间线错误、失败复制与中断标记、保留MP3真实重建及后续复用、无provider模块导入/帮助、三个档位多进程互斥、异常释放和不同项目并行。保留原1080p合成媒体测试和scene_v2基础测试。没有实际新闻成片、人物素材、OCR、真实模型或后台收益测试。

英文完整成片仍未验证；中文0912/0915的真实v3迁移和final哈希不变属于用户提供的本地报告，不冒充本次重做。网站修复只核对diff和测试内容，不以视频199项代替网站45+143。

## 6. 主动研究下一步

具体实施合同见`ON_DEMAND_RESEARCH_DESIGN_20260918.md`：自由问题/已有题目双入口、鉴权幂等、有界搜索和原文读取、来源/主张关系、预算/取消/不确定外部调用恢复、导出既有视频合同，以及按同一快照做真实价值对照。它是可实施设计，不是已经上线的搜索服务；没有引入付费API或多Agent。

## 7. 给本地 Codex 的接续动作

1. 拉取本新分支，以ceacc08为差异基线独立worktree复核。原视频PR #1和网站PR #10不改main；新PR叠加在原视频分支上，避免重复旧diff。先读本文件，再读研究实施合同。
2. 在实际WSL虚拟环境运行`python -m pytest -q tests/`，预期当前新增后为199项（后续增测则记录新计数），不需要注入provider占位；确认jieba路径和已有provider环境都兼容。不要删除Codex原测试或降低断言。
3. 对中文0912/0915副本分别验证：仅final→preview/candidate；仅preview有效→release；preview过期但final有效；修改字号；保留MP3的reuse-tts→reuse-audio。真实运行render_video/mux，不能替换成stub。低档位前后比较全部final文件哈希；release只在副本运行。
4. 用阻断provider加载/网络的方式验证离线复用，断言没有重新合成。新合成真实供应商测试需要另外授权，不为测试自动消费。保留当前v3指纹，不回写历史v2报告。
5. 验证同目录第二个CLI在修改文件之前失败，不同项目正常；不手动删在用锁。中断标记要追根因，不能为了通过直接抹掉；有有效alternate组可自然恢复，否则从原TTS显式重建。
6. 通过后备份并增量同步已确认的`/home/zxhwolfe/.codex/skills/global-hotspot-radar-video/`。除了renderer和测试，必须包含两个新增模块audio_artifacts.py、episode_lock.py；记录源SHA、实际路径、SHA-256并在安装路径复跑。
7. 网站f0f53879继续独立回归；本轮未动其分支。记录全套release原子发布、英文完整成片、Windows锁分支和真实中文排版的未验证边界。不部署、不自动发布、不强推、不覆盖其他会话修改。
8. 后续研发优先按研究合同实现fake适配器+请求/任务合同，再在明确授权下连接现有可用检索能力。不要再只扩展brief提示词来代替搜索执行。

交回：新提交与测试命令、真实副本各链路结果、安装路径/哈希、是否发生任何provider调用、尚未验证事项。不要重复此前已完成的安装路径排查或声称安装即部署网站。
