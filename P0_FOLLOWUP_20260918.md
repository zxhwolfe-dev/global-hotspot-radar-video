# P0 接续复核：把主动研究接到制作与终检

接续基线：`4c89bfc019bbe2c466dda2932e5a2d535b5ee9db`，分支 `gpt/p0-editorial-research-20260917-4c89bfc`，PR #1。原始主分支基线为 `0f13d8152acae28ad2873f4c3a69a4a5a2c56698`。

本次先读取现有提交，保留已经完成的 P0 主动发现、证据记录与预检脚本，不重复创建分支。此次增量只修改三个现有参考文件和本说明；不改渲染器、manifest、音色、生图工具、动效、字幕、缓存、0.8秒片尾，不执行 P1/P2、合并或部署。七个 references 文件的数量与职责不重排。

## [P0][流程一致性] 站外发现不能被旧终检条件否定

**问题引用**：`quality_and_compliance.md / 内容` 原首项为“选题来自本次实时 Radar 大样本扫描，记录扫描量与快照状态”。而接续基线中的 `selection_and_research.md` 已允许站外候选。制作入口和终检还没有明确接上研究记录预检。

**修改**：替换首项为：

> 以本次实时 Radar 大样本扫描为基线，并执行站外主动发现与原文核查；记录实际扫描量、快照状态和失败/不可用情况。最终候选区分 radar/web/mixed，站外题不伪造 Radar ID 或分数，覆盖不全不宣称全网扫描。

在 `production_workflow.md / 视觉策划` 首项加入新项目的预检交接；在 `quality_and_compliance.md / 内容` 加入：

> 新项目已按 `selection_and_research.md` 运行 `check_selection_research.py --require` 并处理错误，结果保存在 `preview/selection_research_check.json`；`STRUCTURE_OK` 只代表结构检查，`NOT_CONFIGURED` 不是完成检查。旧项目不因本规则回写升级。

完整命令与字段仍只有 `selection_and_research.md` 一处定义，不复制另一份 schema。

**理由**：站外好题能进入制作，研究预检也不会只存在于选题文档末尾。失败记录不等于假装已完成搜索，来源结构检查不等于事实核验。

**风险**：这是 Skill 执行流程，不是渲染器新增自动拦截。直接调用旧渲染器仍不会执行研究预检；宿主须遵循工作流。已有渲染兼容性不变。

## [P0][文案自然度] 检查段落功能，而不再堆禁词

**问题引用**：`editorial_platforms.md / 先定新闻重心，不先找金句` 已有“若点评换个题目仍几乎成立”检查，但未明确说明如何检查跨期段落功能、虚构反转对象与同题摘要拼接。`英文 / TikTok 与 Reels` 原有一条列举固定英文套话的删除规则。

**修改**：在 `editorial_platforms.md` 新增“在现有编辑轮次中检查结构性模板”，并同步终检引用。直接执行五项动作：

1. 把标题、封面与开头的承诺对照正文真正回答的问题，不能用一次演示承诺全面替代。
2. 有真实近期稿件时比较最近3–5期的段落功能，不只比词语；保留合理结构，删除仅为补齐冲突、用法、边界或升华槽位的段落。没有旧稿则注明未比对。
3. 只在同一条解释型新闻内做换序、删句诊断，检查是否损失必要关系；不强迫不同新闻不可换序，也不否定快报的事实并列。
4. 反转与纠偏须有真实对象，不虚构“大家都以为”再反驳。普通解释不需要包装成反常识。
5. 具体数字、故事或类比仍须帮助理解主问题；离题细节删除，必要归属、分母和条件保留。

英文套话规则替换为：

> 按句子的实际作用检查英文套话：删去仍不损失事实、关系或语气的空泛反应，复查没有依据的夸赞、伪转折和替观众宣布感受。惯用表达有明确作用时可以保留，不靠固定禁词表或刻意写乱制造自然感。

**理由**：新闻换了名词，结构仍可能像模板；新增检查直接定位信息、关系和取舍，不让“自然”变成使用特定口头禅或故意写粗糙。

**风险**：仍需主编判断。合理并列、惯用表达、必要限定不应误删。并入现有编辑轮次，只记录关键改动；不增加审批表或强制每句填字段。这次没有真实旧稿配对改写、独立首读或观众数据，不能据此宣称留存提升。

## [P0][双语] 隔离受众假设，不只是隔离语言

**问题引用**：`editorial_platforms.md / 中文 / 视频号` 写有“当前受众数据以50岁以上观众超过一半为基线”，英文段落则强调海外语境。若宿主只有旧基线或仅知道语言，仍可能把中文等同于该年龄群、把英文等同于美国，并复用整条中文叙事。

**修改**：新增“双轨受众的共享与隔离”一节，终检指向该节。要求在既有记录说明各版本的平台、市场和观众问题；可以共享核验事实与许可素材，分别选择入口、背景、解释顺序、类比、结尾和互动。

> 中文视频号的“50岁以上过半”只作为当前主账户的已提供基线，不能外推到全部中文用户或抖音。没有本次后台数据就注明沿用基线，不声称已重新读取。

> 英文不自动等于美国。目标市场未知时说明采用国际一般背景，不自行加入当地费用、法规或使用资格结论；必要的地区差异须另行核查。

三轮编辑分别完成；中文译题只用于英文选题确认，默认不进入英文公开资产；明确要求双语成片时遵循当期要求。同一模型先后重写不冒充独立盲审。

**理由**：独立成稿不是把中文翻译后加外国背景；适配50+是降低理解负担，不是居高临下或把每条新闻改成养生、防骗。

**风险**：没有新增后台数据。沿用基线不证明受众结构未变化，也没有给整个网站永久设置50+受众。未增加 manifest 字段。

## [P0][素材资格] 可溯源、可使用先于无水印

**问题引用**：`production_workflow.md / 真实视频与原声窗口 / 第0项` 原文含“发现即弃用换源（无水印镜像 > 官方源）”，随后把去标裁切与对齐比例放在一起。与已有“只预警、不自动重做”和来源权利原则容易产生误读。

**修改**：完整替换第0项：先核原始发布者、素材身份与使用依据，再做全片联系表及四角放大。优先可溯源且权利明确的原始/获授权版本；所有“无水印优先”只适用于权利已明确的候选。自动扫描不删素材、不去标、不重做；平台风险由用户在换源、申诉或保留之间决定。平台驳回或用户要求处理不代替权利核查；不移除应保留的署名、权利或生成内容标识，不用裁切掩盖来源。无使用依据时取得许可、换用合适素材或放弃片段。

保留 AV1 可转 H264 代理后抽帧的制作说明。窗口几何仍按原合同计算，与权利判断分开。终检同步明确“无水印不等于可用”。

**理由**：外观干净不证明来源可信或具备使用资格。避免宿主选择方便下载的镜像，反而失去来源和许可线索。

**风险**：本次未核验任何旧片段的实际授权，也不替特定片段作法律结论。不改变只读扫描行为，不批量替换或重渲染旧片。

## 上游与网页能力：本轮未实现的部分

视频 Skill 的实际入口是全球热点选题雷达，不应把它与开源项目雷达合并成一个排序任务。网站已有 P0 简报修正由其独立 PR 管理：去掉强制七段、在填默认值前识别缺项、保留降级及语言路径。它仍是“已有事件到研究 brief”，不是网站已经具备自主搜索。

建议后续验证一个按需研究入口：允许输入受众、市场、语言及自由问题，而不只接受既有题目 ID；依次使用现有池、差异化站外查询、原文阅读、时间/转载归并、关键反证、候选比较和证据交接。只深研优胜候选，保存实际查询与获取日志；不对每条首页信号都启动多轮模型，不先引入复杂多 Agent。它尚未在本轮实现或部署。

开源项目应先形成“release/changelog 支持的新变化、真实需求、使用条件、演示身份”的候选包，再参与新闻选择。项目值得使用与新闻值得报道是两种判断，星数与作者 demo 不能代替采用量或本人实测。本轮没有改开源雷达排序、许可证或前端，也不是其全链路审计。

网页会话可以负责研究、编稿、制作素材计划和运行可用的文件处理代码，但不等于已接入用户本机的 TTS、创作目录与发布工作台。现有渲染器在导入时就依赖 Akai 的 `ai_douyin_video_pipeline.tts_provider`；没有对应模块、配置、授权音频与素材，不能宣称已经跑通整条成片生产线。本次没有制作合格新闻 MP4。

## 实际验证与未验证

- 本次重新运行 `python -m pytest -q tests/test_selection_research.py`：35项通过。脚本和测试均来自接续基线，Git blob SHA 一致，合成记录不代表真实新闻证据。
- 三个被修改文件先在本地重建并逐一核对原 blob SHA；差异仅为上述 P0 规则。没有把不完整文件回写远端。
- 这次是文档工作流修正，没有新增运行时代码；测试通过不证明改写后稿件自然或传播效果提升。
- 没有执行全库逐行审计、原渲染器全套回归、实际 TTS 听检、真实中英成片、线上抓取或完整网站测试。没有访问用户本机历史视频和后台数据。
- 原 `P0_REVIEW.md` 的测试和编码冒烟记录保留为此前记录，不冒充本次新执行。网站的29项测试同样属于其既有提交记录，不在这里声称重新运行。

## 按收益/成本排序的合并与验收顺序

1. 网站已有 brief 修正整体复核：不能只拿提示词而遗漏3–7项合同、降级处理、locale与缓存版本；在完整网站环境补跑既有相关测试。
2. 视频 PR #1 连同本次三份参考文件一起审查，保留研究预检的兼容边界；合并后同步实际安装的 Skill 副本，而不是只更新 GitHub 文档。
3. 获准制作下一期时先看实际选题/完整稿：检查外部好题能否进入、证据能否追溯、段落是否有本题信息、中英文是否独立；再走原 TTS、预览、候选、release、听检与终检。
4. 样片和后台数据只能用于检验假设；按相同口径记录实际结果后，再决定是否把按需研究服务产品化。P1/P2 另行确认，本轮不执行。
