# P0 内容质量优化与合并说明

审查基线：`0f13d8152acae28ad2873f4c3a69a4a5a2c56698`，2026-09-17。

本提交只处理 P0：发现、研究、选题交接与证据记录，不重构渲染器。已核对仓库目录，阅读 P0 相关规则，检查渲染器、终检与基础测试的相关实现；不是全库逐行审计，也没有读取本机历史项目、听检真实配音或回放既有成片。根提示词里的历史返工数据是已提供的运行事实，不是本次重新实测的结果。

## [P0][发现] Radar 应是扫描基线，不是候选白名单

**问题**：`references/selection_and_research.md / 大样本 Radar 扫描` 详细规定了 `lane=all` 和分页，原第6项是“先做宽筛：过去24小时、跨市场传播……”。但没有把站外主动发现明确接入候选流程。仅对已进入池内的新闻深研，不能弥补池外题材的遗漏。

**修改**：在同文件新增“P0：主动发现，而不只给榜单写摘要”，并修改 `SKILL.md / 必要能力与边界`：

> 当前选题以本次实时 Radar 扫描为基线，同时执行站外主动发现与原文核查；Radar 不是候选白名单。站外题材注明来源，不伪造 Radar ID 或分数，不能用模型记忆或昨日缓存冒充今天。工具不可用时如实披露覆盖限制。

执行入口为 Radar 广覆盖、站外搜索、原始资料与观众疑问；不同语言独立发现，失败和实际读取量如实记录。继续保留现有查重、计数语义与确认模式，不要求三个入口各贡献一题。

**理由**：补的是主动查漏与追问，而不是增加一个没有校准依据的分数，也不是把新闻池改成 AI 工具榜。

**风险**：搜索可能增加时间和调用成本，所以先宽筛少量差异化查询，再只深研优胜题。这里是宿主执行的 Skill 流程，不是已经上线的网站自主联网服务。

## [P0][研究] 阅读、时间与来源独立性必须落实到关键主张

**问题**：现有规则已有“一手优先”和交叉核验，但 `scripts/validate_episode.py` 的选题检查主要检查七个字段是否存在；填满字段不证明读过原文。来源数量也不能证明多份独立确认。

**修改**：扩展同文件的“深研与事实验证”，明确原文阅读、事件/刊发/观察时间、转载归并、对关键判断的反证检索与研究停止条件。新增 `scripts/check_selection_research.py`，在新项目的既有两个 JSON 中增加可选研究记录，命令为：

```bash
python scripts/check_selection_research.py \
  --selection <项目>/selection_report.json \
  --sources <项目>/source_manifest.json --require
```

检查未读/仅标题来源、缺失引用、热度冒充事件证据、同一 URL 标成不同原始来源、推断缺少理由及无时区获取时间。来源独立性未知和摘录阅读保留警告，不伪造第二来源。

**理由**：把能确定检查的缺口提前暴露，而不是到了 MP4 终检才看到一份自报 PASS。

**风险**：`STRUCTURE_OK` 只说明结构和显式引用关系，不能证明网页内容、阅读声明、真实性、版权或作品吸引力。直接调用旧渲染器不会自动运行此检查；Skill 的新制作流程明确要求运行。旧报告默认 `NOT_CONFIGURED`，不是通过，带 `--require` 才要求新版记录。

## [P0][画面] 不把“没有现场视频”误判成“没有事实证据”

**问题**：`编辑筛选` 的原硬淘汰条件包含“没有可验证画面”。它可能使文本证据扎实的新闻输给容易下载但内容弱的素材。

**修改**：该条件改为：

> 既无可用真实画面，也无法以已核验事实设计可读的解释画面。

并明确：原文可以证明事实，生成示意可以帮助理解，但生成示意不是现场证据。截图保留原始像素，既有 Imagen、透明图层和代码合成边界不变。

**理由**：选题不应被“容易找到视频”绑架，也不能让生图制造看似真实的证据。

**风险**：解释图的信息密度、字体和真实性仍需目视审核；本轮没有生成图片或验证具体版式。

## [P0][叙事] 用材料决定讲法，不给每题补齐同一套栏目

**问题**：现有 `editorial_platforms.md` 已有三轮编辑与独立首读，不需要再造一套审批。但上游 brief、历史稿结构或卡片槽位仍可能把新闻拉回同一种讲法。

**修改**：在研究交接中明确本题主问题、最有解释力的细节和自然停点；不要求冲突、机制、个人影响、局限、总结每项都出现。有真实近期稿件才比较最近3–5期的段落功能；没有文件就标为未比对。中文的一遍听懂与英文独立组织分别执行；不增设禁词库。

**理由**：规则已有的优点应落实到交接结果，不是累积同义检查清单。约两分钟是整集目标，不是每题60秒的固定结构。

**风险**：不能由规则修改推断完播率会提高。本次没有旧稿配对改写、真实首读者或后台对照数据。固定招呼、音色、片尾和三轮编辑不变。

## [P0][素材] 权利与溯源先于无水印

**问题**：生产参考中出现“无水印镜像 > 官方源”的优先顺序；脱离许可前提理解，会把表面干净误当成可用。

**修改**：研究阶段明确：

> 素材首先满足来源与使用权条件，再比较水印和版式。任何“无水印优先”只适用于权利已明确的候选；无水印镜像不比可溯源原始材料更可信。

官方公开不等于开放许可，驳回不赋予移除第三方标识的权利；换源、取得许可或放弃片段优先。未改现有只读视觉风险扫描，也未提供绕审技巧。

**理由**：把素材资格判断放在下载和制作之前。

**风险**：本轮未复审既有片段的授权，也未清理所有生产参考中的历史措辞；后续维护需继续统一文档，但不能据此扩大素材使用权。

## 验证与边界

- `pytest -q tests/test_selection_research.py`：35 项通过，使用明确标注的合成测试记录；没有把 example.org 当作新闻实证。
- 新脚本仅用 Python 标准库，不联网、不写输入文件、不调用生成模型。
- 没有改 `render_episode.py`、`validate_episode.py`、`ghr_renderer/`、TTS/动效/字幕/缓存/编码参数。
- 没有运行现有渲染器全套回归，也没有生成合格新闻成片。现有渲染器依赖宿主项目的 TTS 模块与实际素材。
- 本次环境另做了1秒黑帧/静音 H.264 + AAC 编码冒烟，1080×1920、30fps；这只证明环境可编码，不是本生产线的成片验收。

## 合并顺序清单

1. 将本提交作为完整的一组修改审查并合并：`SKILL.md`、选题研究规则、预检脚本、其35项测试与本说明。
2. 更新实际使用的 Skill 副本；仓库合并不会自动更新本机已安装副本。不修改旧项目 JSON，不批量重渲染旧片。
3. 网站 brief 的模板与降级问题在网站仓库独立提交；不能只替换提示词而不检查其数据合同与缓存版本。
4. 下一次获准制作时，用实际选题材料运行研究预检、现有内容审核、原渲染器回归与音画终检；以真实观看/后台数据判断效果。
5. P1（TTS成本与时长闭环、CDN重试/配额等）及 P2（拆分渲染器、归并参考文档）本轮不执行。
